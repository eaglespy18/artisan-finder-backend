# Copy to .env and fill values
PORT=5000

# PostgreSQL - replace with your local values
DB_HOST=localhost
DB_PORT=5432
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_NAME=artisan_finder

# JWT secret for auth
JWT_SECRET=replace_with_a_strong_secret
JWT_EXPIRES_IN=7d
